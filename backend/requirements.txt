# SatQuery AI — Backend Dependencies
# ====================================
# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.12
pydantic==2.9.0

# ML (install torch separately for your CUDA version)
# See: https://pytorch.org/get-started/locally/
torch>=2.4.0
torchvision>=0.19.0
transformers>=4.45.0
accelerate>=0.34.0
qwen-vl-utils>=0.0.8

# Optical-SAR fusion (BIFOLD BigEarthNet v2.0 ResNet-18) -- M4
# NOTE: configilm pulls in lightning-bolts, whose malformed version constraint
# breaks pip>=24.1's strict metadata validation. Install with pip<24.1
# (pip install "pip<24.1") before installing this file, or configilm's
# install will fail with a metadata error.
rasterio>=1.4.0
configilm>=0.7.1
reben-publication

# Image processing
Pillow>=10.4.0
numpy>=1.26.0

# Logging
loguru>=0.7.0

# Testing
pytest>=8.0.0
httpx>=0.27.0
