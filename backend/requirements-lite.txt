# SatQuery local demo / Controller / Model Host (no GPU torch stack).
#
# Supported Python: 3.11, 3.12 (recommended), or 3.13.
# NOT supported: 3.14+ (pydantic-core / PyO3 lack wheels; builds fail).
#
# Direct dependencies are pinned. Transitive deps resolve under these pins
# so Windows pip stays quiet and does not fight uvicorn/fastapi sub-deps.
# Install only into the project venv created by scripts/start-satquery.*
#
# fastapi pinned to 0.115.3, not 0.115.0 — 0.115.3+ pulls starlette>=0.40.0,
# which caps multipart part size (CVE-2024-47874).

fastapi==0.115.3
uvicorn[standard]==0.30.0
python-multipart==0.0.12
pydantic==2.9.0
pydantic-core==2.23.2
Pillow==10.4.0
numpy==2.1.3
pyproj==3.7.0
loguru==0.7.2
httpx==0.27.2
pytest==8.3.3
