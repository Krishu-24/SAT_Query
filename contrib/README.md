# Optional specialist-model work-in-progress (not required to run the demo).

- `change-detection/` — change-detection model contribution
- `optical-sar/` — optical/SAR fusion contribution

The integrated app under `backend/` + `router/` + `frontend/` runs without these.
